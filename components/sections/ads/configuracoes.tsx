import SettingsPage from "@/components/sections/settings-page";

export default function AdsConfiguracoes({ path }: { path: string }) {
  return <SettingsPage path={path} sub="Dados da sua conta anunciante." />;
}
