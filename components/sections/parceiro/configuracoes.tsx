import SettingsPage from "@/components/sections/settings-page";

export default function ParceiroConfiguracoes({ path }: { path: string }) {
  return <SettingsPage path={path} sub="Dados da sua organização parceira." />;
}
