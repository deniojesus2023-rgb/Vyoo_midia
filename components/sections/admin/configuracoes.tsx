import SettingsPage from "@/components/sections/settings-page";

export default function AdminConfiguracoes({ path }: { path: string }) {
  return <SettingsPage path={path} sub="Dados da conta VYOO e equipe de operação." />;
}
